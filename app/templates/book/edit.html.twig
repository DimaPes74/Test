{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <h1>Edit Book</h1>

    <div class="float-end">
        {{ include('book/_delete_form.html.twig') }}
    </div>
    {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}






{#{% extends 'base.html.twig' %}#}

{#{% block title %}Edit Book{% endblock %}#}

{#{% block body %}#}
{#    <h1>Edit Book</h1>#}

{#    <div class="float-end">#}
{#        {{ include('book/_delete_form.html.twig') }}#}
{#    </div>#}
{#    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}#}
{#    {{ form_row(form.title) }}#}
{#    {{ form_row(form.author) }}#}
{#    <div class="form-group">#}
{#        {{ form_label(form.category) }}#}
{#        <div class="input-group">#}
{#            {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}#}
{#            <div class="input-group-append">#}
{#                <button type="button" class="btn btn-outline-primary" id="editCategoryBtn">#}
{#                    ✏️#}
{#                </button>#}
{#                    <form method="post"#}
{#                          id="deleteCategoryForm"#}
{#                          onsubmit="return confirm('Вы уверены, что хотите удалить эту категорию?');"#}
{#                          style="display:inline;">#}
{#                        <button type="submit" class="btn btn-outline-danger" id="deleteCategoryBtn">🗑️</button>#}
{#                    </form>#}
{#                <!-- Добавление категории -->#}
{#                <button type="button" class="btn btn-outline-success" id="addCategoryBtn">➕</button>#}
{#            </div>#}
{#        </div>#}
{#        {{ form_errors(form.category) }}#}
{#    </div>#}

{#    {{ form_row(form.publishedAt) }}#}
{#    {{ form_row(form.description) }}#}
{#    {{ form_row(form.isbn) }}#}

{#    <button class="btn btn-secondary">{{ button_label|default('Save') }}</button>#}
{#    <a href="{{ path('app_book_index') }}" class="btn btn-light">Back to list</a>#}

{#    {{ form_end(form) }}#}

{#    #}{# Модальные окна #}
{#    <div class="modal" id="editCategoryModal" tabindex="-1">#}
{#        <div class="modal-dialog">#}
{#            <div class="modal-content">#}
{#                <div class="modal-header">#}
{#                    <h5 class="modal-title">Редактировать категорию</h5>#}
{#                    <button type="button" class="close" onclick="closeModal('editCategoryModal')">#}
{#                        <span>&times;</span>#}
{#                    </button>#}
{#                </div>#}
{#                <div class="modal-body" id="editCategoryFormContainer">#}
{#                    {% if category %}#}
{#                        {{ render(controller('App\\Controller\\CategoryController::editForm', {'id': categoryId})) }}#}
{#                    {% else %}#}
{#                        <p>Категория не выбрана.</p>#}
{#                    {% endif %}#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
{#    </div>#}

{#    #}{# Стили для модальных окон #}
{#    <style>#}
{#        .modal {#}
{#            display: none;#}
{#            position: fixed;#}
{#            z-index: 1000;#}
{#            left: 0;#}
{#            top: 0;#}
{#            width: 100%;#}
{#            height: 100%;#}
{#            background-color: rgba(0,0,0,0.5);#}
{#        }#}
{#        .modal-content {#}
{#            background-color: #fff;#}
{#            margin: 15% auto;#}
{#            padding: 20px;#}
{#            border: 1px solid #888;#}
{#            width: 80%;#}
{#            max-width: 600px;#}
{#        }#}
{#        .close {#}
{#            float: right;#}
{#            cursor: pointer;#}
{#            background: none;#}
{#            border: none;#}
{#            font-size: 1.5rem;#}
{#        }#}
{#    </style>#}

{#    <script>#}
{#        function openModal(modalId) {#}
{#            document.getElementById(modalId).style.display = 'block';#}
{#            document.body.style.overflow = 'hidden';#}
{#        }#}

{#        function closeModal(modalId) {#}
{#            const modal = document.getElementById(modalId);#}
{#            if (modal) {#}
{#                modal.style.display = 'none';#}
{#                document.body.style.overflow = 'auto';#}
{#            }#}
{#        }#}

{#        document.addEventListener('DOMContentLoaded', function () {#}
{#            const categorySelect = document.querySelector('select[name="book[category]"]');#}
{#            const editBtn = document.getElementById('editCategoryBtn');#}
{#            const deleteBtn = document.getElementById('deleteCategoryBtn');#}
{#            const modalInput = document.querySelector('#editCategoryFormContainer input[name="category[name]"]');#}
{#            const flashMessageContainer = document.querySelector('#flashMessages');#}

{#            // Функция для обновления состояния кнопок редактирования и удаления#}
{#            function updateButtonsState() {#}
{#                if (categorySelect) {#}
{#                    const selectedOption = categorySelect.selectedOptions[0].label;#}
{#                    const categoryId = +(categorySelect.selectedOptions[0].value); // Получаем ID выбранной категории#}

{#                    if (modalInput) {#}
{#                        modalInput.value = selectedOption;#}
{#                    }#}

{#                    const hasValue = categorySelect.value !== '';#}
{#                    editBtn.disabled = !hasValue;#}
{#                    deleteBtn.disabled = !hasValue;#}

{#                    // Если категория выбрана, проверяем, есть ли связанные книги#}
{#                    if (hasValue) {#}
{#                        checkIfCategoryHasBooks(categoryId);#}
{#                        updateDeleteFormAction(categoryId);  // Обновляем action формы для удаления#}
{#                    }#}
{#                }#}
{#            }#}

{#            // Функция для проверки наличия связанных книг#}
{#            function checkIfCategoryHasBooks(categoryId) {#}
{#                if (!categoryId) return;#}

{#                fetch(`/category/check-category-books/${categoryId}`, { method: 'GET' })#}
{#                    .then(response => response.json())#}
{#                    .then(data => {#}
{#                        if (data.hasBooks) {#}
{#                            deleteBtn.disabled = true;#}
{#                            showFlashMessage('error', 'Нельзя удалить категорию, так как с ней связаны книги');#}
{#                        } else {#}
{#                            deleteBtn.disabled = false;#}
{#                            clearFlashMessages();#}
{#                        }#}
{#                    })#}
{#                    .catch(error => {#}
{#                        console.error('Ошибка при проверке категории:', error);#}
{#                    });#}
{#            }#}

{#            // Функция для обновления action формы для удаления#}
{#            function updateDeleteFormAction(categoryId) {#}
{#                if (categoryId) {#}
{#                    const actionDelete = document.querySelector('#deleteCategoryForm');  // Пытаемся найти форму заново#}
{#                    if (actionDelete) {#}
{#                        // Заменяем URL действия формы на правильный путь для удаления категории#}
{#                        actionDelete.action = "{{ path('app_category_delete', {'id': categoryId})}}"; // Ваш маршрут с id категории#}
{#                    } else {#}
{#                        console.error('Form #deleteCategoryForm not found');#}
{#                    }#}
{#                } else {#}
{#                    const actionDelete = document.querySelector('#deleteCategoryForm');#}
{#                    if (actionDelete) {#}
{#                        actionDelete.action = '';  // Если категория не выбрана, очищаем действие формы#}
{#                    }#}
{#                }#}
{#            }#}

{#            // Функция для отображения флеш-сообщений#}
{#            function showFlashMessage(type, message) {#}
{#                const flashMessageContainer = document.querySelector('#flashMessages');  // Найдем элемент заново#}
{#                if (!flashMessageContainer) {#}
{#                    console.error('#flashMessages container not found');#}
{#                    return;#}
{#                }#}

{#                const messageElement = document.createElement('div');#}
{#                messageElement.classList.add(`alert`, `alert-${type}`);#}
{#                messageElement.textContent = message;#}
{#                flashMessageContainer.appendChild(messageElement);#}
{#            }#}

{#            // Очищаем флеш-сообщения#}
{#            function clearFlashMessages() {#}
{#                flashMessageContainer.innerHTML = '';#}
{#            }#}

{#            // Обработчик изменения выбранной категории#}
{#            if (categorySelect) {#}
{#                categorySelect.addEventListener('change', function () {#}
{#                    updateButtonsState();#}
{#                });#}
{#            }#}

{#            // Инициализация состояния кнопок при загрузке страницы#}
{#            updateButtonsState();#}

{#            if (editBtn) {#}
{#                editBtn.addEventListener('click', () => {#}
{#                    if (categorySelect.value) {#}
{#                        openModal('editCategoryModal');#}
{#                    }#}
{#                });#}
{#            } else {#}
{#                console.error('Edit button not found');#}
{#            }#}

{#            if (deleteBtn) {#}
{#                deleteBtn.addEventListener('click', (event) => {#}
{#                    if (!confirm('Вы уверены, что хотите удалить эту категорию?')) {#}
{#                        event.preventDefault(); // Отменить отправку формы, если пользователь не подтвердил#}
{#                    }#}
{#                });#}
{#            } else {#}
{#                console.error('Delete button not found');#}
{#            }#}

{#            // Закрытие модальных окон при клике за пределами модального окна#}
{#            window.onclick = function(event) {#}
{#                document.querySelectorAll('.modal').forEach(modal => {#}
{#                    if (event.target === modal) {#}
{#                        modal.style.display = 'none';#}
{#                        document.body.style.overflow = 'auto';#}
{#                    }#}
{#                });#}
{#            };#}
{#        });#}
{#    </script>#}
{#{% endblock %}#}
